# 参考截图（含内部应用界面，不发布）
refs/

# 🔴 `_verify/` 曾经整个在这儿（注释写的是「开发期检查脚本与输出，不发布」）。
# 2026-09-18 移出来了，理由：
#  ① 那句「与输出」早就不成立 —— 产物全落系统临时目录，目录里一个产物都没有，整个 332K 都是源码；
#  ② 那 464 项门编码了几十条踩坑判据，功能坏了能重写，判据丢了要重新踩一遍；
#  ③ 只在一台机器上 = 单点故障，而且同事 clone 下来跑不了门。
# 门要装什么、怎么跑，见 desktop/_verify/edit/README.md 开头那节。
#
# 🔴 但「脚本」和「输出」要分开：网页版那套门会往 _verify/out/ 写截图，
# 现在是 148 张 PNG / 23MB，那部分继续不进仓。原来那条规则把两者混在一条里，
# 于是为了挡 23MB 的截图，把 300 多 KB 的判据也一起挡掉了。
_verify/out/

.DS_Store
*.bak
*.bak-*
# 🔴 .bak2-/.bak3- 这种带序号的也要挡。原来只写了 *.bak-*，
# 结果 main.js.bak2-203725（47KB 的旧版）差点被 git add -A 当成源码提交进去。
*.bak[0-9]*

# 场景向导本地版 —— 吉吉说先不部署。写进这里是防 `git add -A` / `commit -am` 顺手带上线
wizard.html
_弃-*.html

# 桥的本地依赖（install.sh 在同事机器上自己装）
bridge/node_modules/
